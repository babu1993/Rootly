[package]
name = "getLogs"
version = "0.1.0"
edition = "2021"

[dependencies]
lambda_http = "1.0.0"
layers = { path = "../../layers" }
serde_json = "1.0.149"


tokio = { version = "1", features = ["macros"] }
